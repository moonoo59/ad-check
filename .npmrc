# pnpm + Electron 호환성 설정
#
# 문제: pnpm 가상 스토어(.pnpm/) 사용 시 Electron 런타임이
#        require('electron') 호출을 인터셉트하지 못하고
#        npm 패키지의 실제 내용(바이너리 경로 문자열)을 반환한다.
#
# 해결: node-linker=hoisted 로 npm 방식의 flat node_modules 구조 사용
#       → Electron 내장 모듈 인터셉트가 정상 동작
node-linker=hoisted
